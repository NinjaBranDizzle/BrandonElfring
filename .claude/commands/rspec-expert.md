Review the page at $ARGUMENTS using the rspec-expert agent.

## Instructions

1. First, identify the route, controller action, models, and all code involved in rendering this page.

2. Launch the rspec-expert agent using the Task tool (subagent_type: rspec-expert). Provide it with all identified files and context for the page. The agent should follow all of its own built-in guidelines, recommendations, and concerns for the review.

3. After the agent completes, present the full report to the user.
